# 同一个 Redis IP:Port 上的多券商隔离示例。
# 密码必须替换；应用 REDIS_URL 使用相应用户名。
user default off
user broker_a on >replace-broker-a-password ~broker_a:* +@read +@write +@connection
user broker_b on >replace-broker-b-password ~broker_b:* +@read +@write +@connection

